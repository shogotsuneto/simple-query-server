# Test queries configuration
queries:
  test_query:
    sql: "SELECT 'test' as message, :id as id"
    params:
      - name: id
        type: int
        
  simple_test:
    sql: "SELECT 'hello world' as greeting"
    params: []